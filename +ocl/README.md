This folder contains the public API that can be called by the user. At the moment
these function are wrappers to the Core functions. Eventually direct call to
the Core functions will be deprecated, and only the API in the +ocl package
should be called.
